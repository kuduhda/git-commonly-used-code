git switch 分支名        //切换到所需要的分支
git ls-files                    //查看分支中的文件
git rm -r 文件名          //删除工作区文件，并且将删除文件提交到暂存区，因为是删除文件夹所以要添加一个“-r”
git commit -m "delete 文件名"    //提交到版本库并删除文件

此时再用第二个命令查看分支里面的文件已经被删除了